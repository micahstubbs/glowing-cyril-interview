<%= form_for(@price) do |f| %>
  <% if @price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@price.errors.count, "error") %> prohibited this price from being saved:</h2>

      <ul>
      <% @price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :curve_id %><br />
    <%= f.text_field :curve_id %>
  </div>
  <div class="field">
    <%= f.label :month_id %><br />
    <%= f.text_field :month_id %>
  </div>
  <div class="field">
    <%= f.label :settle %><br />
    <%= f.text_field :settle %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
